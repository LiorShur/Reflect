// Stub — milestone 3. RTDB onWrite triggers that drive the orchestrator.
// Wiring lands once D1/D4 are in place.

export function registerTriggers(): void {
  // Intentionally empty — concrete triggers added per milestone.
}
